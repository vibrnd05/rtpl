# Copy to frontend/.env.local and fill in.

# Base URL of the Express API in backend/. Server-side only — it is read in a
# server action, never shipped to the browser, so it must NOT be NEXT_PUBLIC_*.
BACKEND_URL=http://localhost:4000
